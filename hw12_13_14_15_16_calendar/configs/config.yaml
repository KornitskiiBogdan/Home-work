logger:
  level: "info"

http:
  host: "0.0.0.0"
  port: 8080

grpc:
  host: "0.0.0.0"
  port: 9090

storage:
  type: "sql"
  db:
    host: "postgres"
    port: 5432
    user: "postgres"
    password: "password"
    dbname: "backend"
    sslmode: "disable"
